# user data — never committed
sessions.json
exports/
logs/
ai_config.json
tasks.json
insights.json
ai_reports.json
dpo_dataset.jsonl
preview.html
*.png
!assets/*.png
!screenshots/*.png
!docs/**/*.png

# python
__pycache__/
*.pyc
.venv/
venv/

# build artifacts (the spec IS tracked — the build must be reproducible from git)
build/
dist/
SessionLogger.exe
OpenTimeLogger.exe

# local tooling
.playwright-mcp/